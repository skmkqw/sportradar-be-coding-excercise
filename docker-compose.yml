services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sports-calendar-db
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=${DB_PASSWORD}
    ports:
      - "${DB_PORT}:1433"
    volumes:
      - mssql-data:/var/opt/mssql
      - ./database/init-db.sql:/usr/config/init-db.sql

  backend:
    image: sports-calendar-api
    container_name: sports-calendar-api
    build:
      context: ./backend/src
      dockerfile: SportsCalendar.Api/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - CONNECTION_STRING=Server=sqlserver,1433;Database=${DB_NAME};User Id=${DB_USER};Password=${DB_PASSWORD};TrustServerCertificate=True;
    ports:
      - "5000:8080"
    depends_on:
      - sqlserver
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  frontend:
    image: sports-calendar-frontend
    container_name: sports-calendar-frontend
    build:
      context: ./frontend/sports-calendar
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
      - INTERNAL_API_BASE_URL=http://backend:8080
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  mssql-data:
